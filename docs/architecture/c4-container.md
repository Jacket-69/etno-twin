# C4 level 2 — Containers

TODO — draw after ADR-0001. What lives here depends directly on the composition style:
an artifact pipeline yields stage executables plus a shared storage layout; in-memory
orchestration yields a single library with adapters.
